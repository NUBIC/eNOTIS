FIRST TIME ONLY

sudo gem install -v=2.3.2 rails

create ~/.gemrc

---
:update_sources: true
gem: --no-ri --no-rdoc
:bulk_threshold: 1000
:backtrace: false
:verbose: true
:benchmark: false
:sources:
- http://gems.rubyforge.org
- http://gems.github.com
- http://download.bioinformatics.northwestern.edu/gems/


# for sudo gem install libxml-ruby

sudo yum install ruby-devel # already installed

sudo yum install zlib # already installed
sudo yum install zlib-devel

sudo yum install libxml2 # already installed
sudo yum install libxml2-devel

# works!


# for sudo gem install sqlite3-ruby

sudo yum install sqlite3-devel # didn't help
sudo yum install rubygem-sqlite3-ruby

# works!