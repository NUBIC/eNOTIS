- @javascripts = ["jQuery-1.3.2.min" , "jquery-ui-1.7.2.min" ,"jrails", "facebox", "jquery.dataTables.min"]
- @stylesheets = ["reset", "compiled/default", "facebox", "compiled/datatables"]

- content_for :head do
  :javascript
    $(document).ready(function(){$(".display").dataTable();});

#my_events
  .heading My Events
  %table.display
    %thead
      %th Study ID
      %th Subject Name
      %th Event Description
      %th Event Date/Time
    %tbody  
      - @events.each do |e|
        %tr
          %td= link_to e.involvement.study.irb_number, study_path(e.involvement.study) || "(no id)"
          %td= facebox_link_to(e.involvement.subject.name, :url => subject_url(e.involvement.subject) , :method=>:get)
          %td= e.description
          %td= "#{time_ago_in_words(e.created_at)} ago"