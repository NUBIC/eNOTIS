#my-events
  .heading My Events
  %ul
    - @events.each do |e|
      %li
        = e.involvement.study.irb_number
        \-
        = e.involvement.subject.name
        = e.description
        = "#{time_ago_in_words(e.created_at)} ago"