- @view_javascripts = ["jquery-1.3.2.min", "jquery-ui-1.7.2.min" ,"jrails", "facebox", "jquery.dataTables-1.5beta.min"]
  
- content_for :head do
  :javascript
    $(document).ready(function(){$(".display").dataTable({bServerSide: true, sAjaxSource: '/studies.json' });});

#study_list
  .heading= "All Studies (#{@studies_count})"
  = render :partial => 'partials/study_table'
