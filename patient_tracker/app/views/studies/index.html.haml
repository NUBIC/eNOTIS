- @stylesheets = %w(reset compiled/default compiled/datatables)
- @javascripts = %w(jquery-1.3.2.min jquery.dataTables-1.5beta.min)
- content_for :head do
  :javascript
    $(document).ready(function(){$(".display").dataTable({bServerSide: true, sAjaxSource: '/studies.json' });});

#study_list
  .heading= "All Studies (#{@studies.size})"
  = render :partial => 'partials/study_table'