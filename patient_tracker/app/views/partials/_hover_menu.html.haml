%ul#menu
  %li
    #app_name eNOTIS
  %li
    #protocol_search
      -form_tag(search_studies_path, :method => :get) do 
        Find protocol
        =text_field_tag("study_id")
        =submit_tag("Find")
  %li
    #subject_search
      -form_tag(search_subjects_path,:method => :get) do
        Find subject
        =text_field_tag("mrn")
        =submit_tag("Find")
  %li
    #current_user
      =link_to("Logged in as:",registrations_path)
      &nbsp;
      =link_to(user.netid, logout_authentication_path)
.clear
  &nbsp;
