# To clear your gems, use this command, removing any the lines for any gems you don't have (it'll choke)

sudo gem uninstall --all --ignore-dependencies --executables \
builder \
chriseppstein-compass \
columnize \
cucumber \
diff-lcs \
faker \
haml \
haml-edge \
httpclient \
libxml-ruby \
linecache \
mechanize \
mechanize-ntlm \
nokogiri \
polyglot \
populator \
rcov \
rspec \
rspec-rails \
ruby-debug \
ruby-debug-base \
soap4r \
sqlite3-ruby \
term-ansicolor \
thoughtbot-factory_girl \
treetop \
webrat \
ZenTest


# To add the lib exceptions to haml run: (this file gets re-created after haml inits)
git co vendor/plugins/haml/init.rb

# Mechanize-ntlm isn't available from a gem repository, so the following won't work
# config.gem "mechanize-ntlm", :version => '0.9.1'
# download from http://www.mindflowsolutions.net/2009/5/21/ruby-ntlm-mechanize

sudo gem install mechanize-ntlm-0.9.1

# Nokogiri depends on libxml2, and complains loudly every time it uses the default 2.6 that comes with OSX
# Follow http://jamesclarke.info/notes/libxml2/ and download http://xmlsoft.org/sources/libxml2-2.7.3.tar.gz
# WARNING! Using Leopard, stop after "sudo make install", don't rename/remove the default libxml2

# Now, install gems

uncomment the config.gem lines in config/environment.rb, config/environments/development.rb, and config/environments/test.rb

sudo rake gems:install
sudo rake gems:install RAILS_ENV=test