-form_for(@event_type,:url=>study_event_type_path(@study,@event_type)) do |f|
  = f.label :name
  = f.text_field :name
  %br
  = f.label      :description
  = f.text_field :description
  %br
  =f.submit "update"
