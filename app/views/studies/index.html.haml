#my_studies
  .heading= "My Studies (#{@my_studies.total_entries} in total)"
  = render :partial => 'partials/study_table', :locals => {:studies => @my_studies}
  #study_information.overlay
    .contentWrap